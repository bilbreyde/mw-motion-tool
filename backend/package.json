{
  "name": "mw-motion-tool-backend",
  "version": "1.0.0",
  "description": "Azure Functions backend for DW Motion Tool",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && func start",
    "dev": "npm run build && func start --verbose"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "openai": "^4.67.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  },
  "main": "dist/src/index.js"
}
